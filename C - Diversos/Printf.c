#include<stdio.h>
#include<stdlib.h>

int main()
{
    printf("Aspas duplas: \"\n"); // para exibir aspas ou contrabarra é necessario colocar uma contrabarra antes
    printf("Barra: \\");          /* isso se deve ao fato do computador entender de forma diferente esses simbolos
                                  por isso é necessario indicar ao computador o que fazer por meio da contrabarra */
                                  // há duas formas de se comentar o codigo. O primeiro é por meio do simbolo: //
                                  // já a segunda maneira é delimitar tudo do inicio ao fim usando: /* e */,respectivamente


    return 0;
}
